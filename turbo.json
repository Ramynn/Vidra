{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "cache": true
    },
    "lint": {
      "cache": true
    },
    "dev": {
      "outputs": ["dist/**", "build/**"],
      "dependsOn": ["^dev"],
      "cache": false
    },
    "format": {
      "cache": false
    }
  }
}
